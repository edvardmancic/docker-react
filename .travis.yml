sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t edvardmancic/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true edvardmancic/docker-react npm run test -- --coverage